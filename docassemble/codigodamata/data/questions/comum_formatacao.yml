code: |
  # teste_virgula valida existência da varíavel e adiciona uma vírgula
  def teste_virgula(x):
    if x == "":
      return ''
    else:
      return x + ', '

  # teste_virgula_da para adicionar variáveis duplas (ou string + var, var + string) validando x
  def teste_virgula_dx(x,y):
    if x == "":
      return ''
    else:
      return x + y

  # teste_virgula_dp para adicionar variáveis duplas (ou string + var, var + string) validando y
  def teste_virgula_dy(x,y):
    if y == "":
      return ''
    else:
      return x + y

  # teste_virgula_dz para adicionar variáveis triplas, validando y
  def teste_virgula_dz(x,y,z):
      if y == "":
        return ''
      else:
        return x + y + z